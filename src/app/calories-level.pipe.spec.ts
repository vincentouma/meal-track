import { CaloriesLevelPipe } from './calories-level.pipe';

describe('CaloriesLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new CaloriesLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
